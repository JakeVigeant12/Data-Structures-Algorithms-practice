public class GridPaths {
    static int[][] grid;
    static int[][] sums;

    public static void main(String[] args) {
        sums = new int[grid.length][grid[0].length];
        maxSum(grid);
    }

    public static int maxSum(int[][] grid) {
        for (int row = 0; row < grid.length; row++) {
            for (int col = 0; col < grid[0].length; col++) {

            }
        }
    }
}
